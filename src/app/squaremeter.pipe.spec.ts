import { SquaremeterPipe } from './squaremeter.pipe';

describe('SquaremeterPipe', () => {
  it('create an instance', () => {
    const pipe = new SquaremeterPipe();
    expect(pipe).toBeTruthy();
  });
});
